import Paroller from "@/components/inner-page/paroller";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Inner Product Suxnix - Health Supplement React Next js Template",
};
const index = () => {
   return (
      <Wrapper>
         <Paroller />
      </Wrapper>
   )
}

export default index
